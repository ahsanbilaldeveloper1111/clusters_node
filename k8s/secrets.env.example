# Copy to k8s/overlays/local/secrets.env (or production) — never commit real secrets
JWT_SECRET=change-me-in-production-use-long-random-string
POSTGRES_PASSWORD=app_secret
DATABASE_URL=postgresql://app_user:app_secret@postgres-service:5432/enterprise_db
