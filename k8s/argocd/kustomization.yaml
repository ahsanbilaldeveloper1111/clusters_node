apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - appproject.yaml
  - applications/enterprise-app.yaml

# root.yaml (app-of-apps) is applied separately after Argo CD is installed
