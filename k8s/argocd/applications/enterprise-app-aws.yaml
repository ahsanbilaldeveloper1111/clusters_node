apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: enterprise-app-aws
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: enterprise-advanced-app
    app.kubernetes.io/environment: aws
spec:
  project: enterprise
  source:
    repoURL: git@github.com:ahsanbilaldeveloper1111/clusters_node.git
    targetRevision: main
    path: k8s/overlays/gitops-aws
  destination:
    server: https://kubernetes.default.svc
    namespace: enterprise-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=false
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  ignoreDifferences:
    - group: ""
      kind: Secret
      name: app-secrets
      namespace: enterprise-app
      jsonPointers:
        - /data
    - group: batch
      kind: Job
      jsonPointers:
        - /status
