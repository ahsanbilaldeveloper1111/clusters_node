# Scrape Prometheus metrics from the Node backend (/metrics)
global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: enterprise-backend
    metrics_path: /metrics
    static_configs:
      - targets: ['backend:3000']
        labels:
          service: enterprise-backend
          env: local
